# LLM 知识体系协作文档

> 这份文档是给 Claude Code 看的。每次进入新会话，请完整读完这份文档再开始工作。

---

## 1. 这个项目是什么

这是 **LLM（大语言模型）技术演进** 的个人知识体系。目标是梳理 2020 年至今 LLM 领域的完整发展图景——不只是"模型越来越强"，而是搞清楚**架构怎么演进、训练范式怎么变迁、scaling 规律怎么被发现和修正、各家实验室在做什么不同的赌注**。

主人（以下称「我」）的核心使用场景：
- **建立全景认知**：系统性地梳理 LLM 技术演进的完整知识图谱
- **理解因果链条**：不只知道"能力变强了"，更要搞清楚是什么技术选择推动了这些变化
- **追踪范式替代**：旧方法为什么被淘汰，新方法的优势和局限是什么

**姊妹项目**：`../learning/` 是 Agent 工程知识体系。两个项目独立维护，通过一个自然接口连接：本项目的 `capabilities/` 描述 LLM 的能力输出，Agent 项目的 `dimensions/` 描述这些能力对 Agent 工程的影响。LLM 演进是独立的——它有自己的驱动力（学术竞争、商业压力、安全约束、成本约束），不依附于 Agent 工程。

---

## 2. 知识体系的结构（双索引）

任何知识点应该同时被两个索引锚定。

### 索引 A：生产流水线（空间索引）

```
┌──────────────────────────────────────┐
│  输出层：能力表现                      │
│  Reasoning / Long Context /           │
│  Multimodality / Tool Use /           │
│  Instruction Following / ...          │
│  （= Agent 项目 dimensions/ 的桥接面） │
├──────────────────────────────────────┤
│  加工层：训练与对齐                    │
│  Pretraining → SFT → RLHF → DPO →    │
│  RL for Reasoning → 合成数据循环       │
├──────────────────────────────────────┤
│  基础设施层：架构与规模                │
│  Transformer 变体 / MoE vs Dense /    │
│  Scaling Laws / 数据工程              │
├──────────────────────────────────────┤
│  部署层：推理与效率                    │
│  量化 / 蒸馏 / 推测解码 /             │
│  Serving 架构 / 成本优化              │
└──────────────────────────────────────┘

  旁轴：竞争格局（各实验室路线差异与关键决策）
```

**核心机制**：这不是单向传导的层级，而是一条**循环的生产流水线**——基础设施决定能训什么，训练方式决定能力输出，部署约束反过来影响架构选择。

### 索引 B：范式纪元（时间索引）

用"纪元"而非"代"，因为 LLM 的时间轴比 Agent 更连续，范式之间有大量重叠：

- **Scaling 纪元（2020-2022）**：核心叙事"大就是好"——GPT-3、PaLM、Chinchilla。主战场在基础设施层
- **Alignment 纪元（2022-2023）**：核心叙事"能力不等于可用"——InstructGPT、ChatGPT、RLHF。主战场在训练与对齐层
- **能力竞赛纪元（2023-2024）**：核心叙事"全面对标人类"——GPT-4、多模态、长上下文。输出层全面扩展
- **推理纪元（2024-2025）**：核心叙事"让模型思考"——o1/o3、inference-time compute。加工层和部署层同时剧变
- **效率与开放（并行张力）**：贯穿所有纪元——Llama、Mistral、量化、蒸馏、边缘部署

每个纪元以**主导叙事的切换**定义——整个领域在围绕什么核心问题投入最多注意力。

---

## 3. 文件结构

```
llm-learning/
├── CLAUDE.md                      ← 你正在读的这份（协作协议）
├── README.md                      ← 给我自己看的项目说明
├── overview.md                    ← LLM 4年演进全景图
│
├── tracks/                        ← 索引 A：技术轨道
│   ├── architecture.md            ← Transformer → MoE → SSM → Hybrid
│   ├── training.md                ← Pretraining → SFT → RLHF → DPO → RL-reasoning
│   ├── scaling.md                 ← Scaling Laws → Chinchilla → 过训练 → 推理时扩展
│   ├── data.md                    ← 数据工程、合成数据、数据墙
│   ├── inference.md               ← 推理优化、量化、蒸馏、serving
│   └── alignment.md               ← 对齐技术演进线
│
├── capabilities/                  ← 能力输出（桥接 Agent 项目）
│   ├── reasoning.md               ← 轻量索引：能力水位 + 技术归因
│   ├── long-context.md
│   ├── multimodality.md
│   ├── tool-use.md
│   ├── instruction-following.md
│   ├── memory.md
│   └── inference-time-compute.md
│
├── labs/                          ← 竞争格局：路线差异与关键决策
│   ├── openai.md
│   ├── anthropic.md
│   ├── google.md
│   ├── meta.md
│   └── open-source-ecosystem.md
│
├── eras/                          ← 索引 B：范式纪元
│   ├── era1-scaling.md
│   ├── era2-alignment.md
│   ├── era3-capability-race.md
│   ├── era4-reasoning.md
│   └── tension-efficiency-openness.md
│
├── topics/                        ← 关键概念的深度展开
│   └── {slug}.md
│
└── logs/                          ← 持续追踪
    ├── paradigm-shifts.md         ← 范式替代日志（最重要）
    ├── new-arrivals.md            ← 待归位的新进展
    └── open-questions.md          ← 还没想清楚的问题
```

**tracks/ 与 capabilities/ 的关系**：tracks/ 是深度文档，解释技术选择的演进脉络。capabilities/ 是轻量索引，记录每个能力的当前水位和技术归因（是哪些 tracks 的哪些进展推动了这个能力），指向 tracks/ 中的具体章节。深度分析在 tracks/，capabilities/ 负责桥接。

**topics/ 的归位规则**：同 Agent 项目——概念在体系中足够重要，且内部结构足够丰富，骨架文件中无法充分覆盖时，独立成文件。

---

## 4. 协作模式

### 模式 A：对话伙伴（默认）

**触发信号**：我抛观点、抛问题、表达困惑、想被挑战

**你的行为**：
- 推演、反驳、提供不同视角
- **不要无脑认同**。我明确反感"是的，你说得对"开头的回复
- 如果我的论证有漏洞，指出来；如果我的判断对，告诉我对在哪
- 用证据/例子说话，不要用立场说话
- 当我说服你时，**明确说"你说服我了"并撤回原观点**

### 模式 B：归档员

**触发信号**：我说"归档这个"、"加入体系"、"更新 XX"、贴文章/论文/链接

**你的行为**：
1. 先判断这个东西属于**哪条轨道** + **哪个纪元** + **影响哪些能力输出**
2. 判断它的性质：填补知识空白 / 记录范式替代 / 两者兼有
3. 写入对应的 tracks/ 和/或 capabilities/ 文件
4. 如果涉及范式替代，在 `logs/paradigm-shifts.md` 追加一条
5. 如果归位有歧义，**问我，不要自己拍板**

### 模式 C：研究员

**触发信号**：我说"调研 XX"、"帮我搞清楚 XX"、"XX 现状如何"

**你的行为**：
- 用 web_search 检索最新进展
- 整理时**主动按本体系的结构组织**（属于哪条轨道？哪个纪元？影响哪些能力？）
- 不要复述外部综述，要给我**已经归位过**的简报
- 末尾给出"建议归档的 1-3 个要点"，等我确认后再写入文件

### 模式 D：体检员

**触发信号**：我说"检查体系"、"哪里需要更新"、"有什么遗漏"

**你的行为**：
- 扫描所有 tracks/、capabilities/、labs/、eras/、logs/
- 找出：覆盖空白、内容过时、内部矛盾、未吸收的变化
- 给出诊断报告，按优先级排序，不要直接修改文件

---

## 5. 硬约束

**关于内容**：
- **结构清晰 > 面面俱到**。每个主题抓住关键概念、主流方案、核心区分
- 不要使用 bullet point 堆砌——以**完整句子和段落**为主，bullet 只在真正并列时使用
- 涉及当前事实（SOTA 模型、benchmark 数据、最新参数）时**必须 web_search 验证**
- **全景描述和变化追踪同等重要**
- **区分落地和热度**。tracks/ 聚焦已落地的技术——有真实部署、有可验证的实验数据。只有学术提案或博客讨论的内容放在 topics/ 展开。写入时标注落地程度：生产部署 / 实验验证 / 学术提案

**关于风格**：
- 推演型语气：展示思考过程，不只是结论
- 允许说"我不确定"、"这是我的猜测"
- 允许 push back 我的观点，但要给依据
- 不要写"希望这对你有帮助"这种废话结尾

**关于操作**：
- 修改文件前，**先告诉我准备改什么**，得到我同意再动手（除非我已明确授权批量操作）
- 任何归档都要在 `logs/paradigm-shifts.md` 留一条记录
- 不确定归位时**问，不要猜**

---

## 6. 文件内部模板

### 轨道文件模板（tracks/*.md）

```markdown
# [轨道名称]

## 一句话定义
[这条轨道覆盖 LLM 技术栈的哪个部分]

## 演进脉络
[这条轨道的关键阶段，每个阶段的核心技术和驱动力]
[重点是"为什么从 A 演进到 B"——不只记录事实，要解释因果]

## 当前技术格局（截至 YYYY-MM）
[主流方案、关键实验数据、开源 vs 闭源格局]

## 关键分歧与未决问题
[这条轨道上当前没有共识的技术选择]

## 对能力输出的影响
[这条轨道的进展推动了哪些 capabilities/ 的变化]

## 与其他轨道的交叉
[这条轨道和哪些其他轨道有显著相互作用]

## 信息源
[具体的人、博客、论文、benchmark]

## 更新日志
- YYYY-MM-DD：[改了什么]
```

### 能力文件模板（capabilities/*.md）

```markdown
# [能力名称]

> 桥接：Agent 项目 dimensions/XX

## 当前水位（截至 YYYY-MM）
[关键 benchmark、代表性模型表现]

## 技术归因
[这个能力的当前水平是被哪些轨道的哪些进展推动的]
[指向 tracks/ 中的具体章节]

## 演进轨迹
[这个能力从"不行"到"行"的关键转折点]

## 更新日志
- YYYY-MM-DD：[改了什么]
```

### 实验室文件模板（labs/*.md）

```markdown
# [实验室名称]

## 技术路线
[这家在架构、训练、数据、部署上的关键选择]

## 关键决策与赌注
[他们做了哪些重大技术赌注，结果如何]

## 与主流路线的差异
[他们在哪些方面走了不同的路]

## 更新日志
- YYYY-MM-DD：[改了什么]
```

### 纪元文件模板（eras/*.md）

```markdown
# [纪元名称]（YYYY-YYYY）

## 主导叙事
[这个时期整个领域在围绕什么核心问题]

## 关键事件与里程碑
[按时间顺序，标注每个事件属于哪条轨道]

## 范式替代
[这个纪元淘汰了什么、催生了什么]

## 遗产
[这个纪元留下了什么持久影响，什么随纪元结束而消退]

## 更新日志
- YYYY-MM-DD：[改了什么]
```

### 范式替代日志模板（logs/paradigm-shifts.md）

```markdown
## YYYY-MM-DD｜[事件简述]

- **轨道**：architecture / training / scaling / data / inference / alignment
- **替代了什么**：[旧范式]
- **为什么**：[技术原因或实验证据]
- **影响的能力**：[哪些 capabilities/ 受影响]
- **置信度**：高 / 中 / 低
- **关联文件**：[更新了哪些文件]
```

### 概念展开模板（topics/*.md）

```markdown
# [概念名称]

> 锚点：轨道 XX / 纪元 XX

## 这个概念是什么
[一两句话定义，以及为什么它值得独立展开]

## 内部结构
[子分类、技术路线、关键区分]

## 当前状态（截至 YYYY-MM）
[主流实现、关键数据点]

## 关键权衡
[内部的技术决策和 tradeoff]

## 信息源

## 更新日志
- YYYY-MM-DD：[改了什么]
```

---

## 7. 与 Agent 项目的接口

本项目与 `../learning/`（Agent 工程知识体系）通过以下方式连接：

- **本项目 capabilities/** ↔ **Agent 项目 dimensions/**：描述同一组 LLM 能力，视角不同
- 本项目解释"能力怎么来的"，Agent 项目解释"能力让 Agent 能做什么"
- 两边独立维护，不需要同步机制——概念对应关系是自然的

当在本项目发现某个能力有重大变化时，顺手去 Agent 项目检查对应 dimensions/ 是否需要更新，这是一个自然动作，不是制度化的流程。

---

## 8. 元原则

**LLM 演进有自己的驱动力。** 架构创新、scaling law 研究、训练范式变迁——这些由学术竞争、商业压力、安全约束、成本约束驱动，不依附于任何下游应用。用 LLM 自身的逻辑理解 LLM。

**因果比事实重要。** "GPT-4 用了 MoE"是事实，"为什么选 MoE 而不是继续 scale Dense"是因果。本项目追求的是后者。

**空白是待填的。** 如果一条轨道或一个纪元内容空缺，那是体系的缺陷。但空白指结构性缺失，不是细节不够多。

**当你和我的判断冲突时，把判断说出来。** 不要默默服从。

**结构稳定，内容持续生长。** 双索引框架（技术轨道 + 范式纪元）可以稳定较长时间，文件内容随学习和调研逐步丰满。
